apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mugundhan.fullname" . }}-test-connection"
  labels:
    {{- include "mugundhan.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mugundhan.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
