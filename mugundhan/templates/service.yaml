apiVersion: v1
kind: Service
metadata:
  name: {{ include "mugundhan.fullname" . }}
  labels:
    {{- include "mugundhan.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mugundhan.selectorLabels" . | nindent 4 }}
